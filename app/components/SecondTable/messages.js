/*
 * SecondTable Messages
 *
 * This contains all the text for the SecondTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SecondTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SecondTable component!',
  },
});
