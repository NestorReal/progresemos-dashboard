/*
 * FormCsv Messages
 *
 * This contains all the text for the FormCsv container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FormCsv';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FormCsv container!',
  },
});
